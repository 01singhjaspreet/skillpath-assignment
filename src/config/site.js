/** @typedef {{ catalogHeading: string, showRefundableBadges: boolean }} SiteConfig */

/** @type {SiteConfig} */
export const siteConfig = {
  catalogHeading: "Choose what you’ll ship next",
  showRefundableBadges: true,
}
